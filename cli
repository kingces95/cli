#!/usr/bin/env bash-cli-shim
cli() {
    if [ ! -v CLI_LOADER_SHIM_ROOT_CLI ]; then
        readonly CLI_LOADER_SHIM_ROOT_CLI="$( cd $(dirname ${BASH_SOURCE}); pwd)/src"
    fi
    
    BASH_ARGV0="${CLI_LOADER_SHIM_ROOT_CLI}"
    cli::loader::shim "${FUNCNAME}" "$@"
}
