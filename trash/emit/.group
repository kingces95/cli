#!/usr/bin/env CLI_NAME=cli bash-cli-part

cli::emit::help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}

Description

EOF
}

self_test() {
    cli emit packed .group --self-test

    cli emit function --self-test
    cli emit global-variable --self-test
}
