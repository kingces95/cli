#!/usr/bin/env CLI_NAME=cli bash-cli-part

cli::bash::type::help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}

Description
EOF
}

cli::bash::type::main() {
    :
}

cli::bash::type::self_test() {
    cli bash type get-info --self-test
}
