#!/usr/bin/env CLI_NAME=cli bash-cli-part

help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}

Description
EOF
}

cli::bash::variable::declaration::main() {
    :
}

cli::bash::variable::declaration::self_test() {
    cli bash variable declaration get-info --self-test
}
