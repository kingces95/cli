#!/usr/bin/env CLI_TOOL=cli bash-cli-part

cli::bash::array::help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}

Description
EOF
}

cli::bash::array::main() {
    :
}

cli::bash::array::self_test() {
    cli bash array literal --self-test
    cli bash array pop --self-test
}
