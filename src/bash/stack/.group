#!/usr/bin/env CLI_NAME=cli bash-cli-part

help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}

Description
EOF
}

main() {
    :
}

self_test() {
    echo cli bash stack call --self-test
    echo cli bash stack hidden-attribute --self-test
    echo cli bash stack process --self-test
    echo cli bash stack trace --self-test
}
