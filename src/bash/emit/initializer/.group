#!/usr/bin/env CLI_TOOL=cli bash-cli-part

cli::bash::emit::initializer::help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}

Description
EOF
}

cli::bash::emit::initializer::main() {
    :
}

cli::bash::emit::initializer::self_test() {
    cli bash emit initializer string --self-test
    cli bash emit initializer array --self-test
    cli bash emit initializer map --self-test
}
