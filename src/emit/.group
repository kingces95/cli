#!/usr/bin/env bash
source $(cli loader)
cli::import_parent_group

help() {
    cat << EOF | cli::help::global
Command
    ${CLI_COMMAND}
EOF
}

export() {
    cli::export::import_parent_group
}

cli::main "$@"