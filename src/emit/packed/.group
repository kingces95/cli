#!/usr/bin/env CLI_NAME=cli bash-cli-part

help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}
EOF
}

cli::emit::library_name() {
    local name=$*
    echo "${name// /::}"
}

cli::emit::packed::main() {
    :
    cli::export::import cli_emit
    cli::export cli emit
}

cli::main "$@"