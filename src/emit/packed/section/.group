#!/usr/bin/env CLI_NAME=cli bash-cli-part

help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}
EOF
}

cli::emit::section() {
    echo \#
    echo \# $1
    echo \#
    while read -r; do echo "${REPLY}"; done
    echo
}

cli::emit::packed::section::main() {
    :
    cli::export::import cli_emit
    cli::export cli emit
}

cli::main "$@"