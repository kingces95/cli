#!/usr/bin/env CLI_NAME=cli bash-cli-part

cli::eg::help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}

Description

EOF
}

cli::eg::main() {
    : # special loader case; export nothing
}

cli::eg::self_test() {
    cli eg recurse --self-test
    cli eg simple --self-test
}
