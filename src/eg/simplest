#!/usr/bin/env CLI_NAME=cli bash-cli-part

cli::eg::simplest::help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]} 

Summary    
    Sample command to test Domain Specific Language.
EOF
}

cli::eg::simplest::main() {
    :    
}

cli::eg::simplest::self_test() {
    return
}   
