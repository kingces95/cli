#!/usr/bin/env CLI_NAME=cli bash-cli-part

help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}
EOF
}

main() {
    : # special loader case; export nothing
}

self_test() {
    echo cli process parent --self-test
    echo cli process tree --self-test
}
