#!/usr/bin/env CLI_NAME=cli bash-cli-part

help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}
EOF
}

main() {
    :
}

self_test() {
    # cli core type emit --self-test
    cli core type from-flags --self-test
    cli core type to-flags --self-test
    cli core type get --self-test
    cli core type load --self-test
    cli core type resolve --self-test
    cli core type unmodified --self-test
}