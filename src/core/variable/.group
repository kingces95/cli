#!/usr/bin/env CLI_NAME=cli bash-cli-part

help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}
EOF
}

cli::meta::declare() {
    :
}

cli::core::variable::_group::main() {
    :
    # cli::export cli core variable
}
