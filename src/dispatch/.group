#!/usr/bin/env CLI_NAME=cli bash-cli-part

help() {
    cat << EOF
Command
    ${CLI_COMMAND[@]}

Description

EOF
}

cli::dispatch::main() {
    : # special loader case; export nothing
}

cli::dispatch::self_test() {
    cli main parse --self-test
}
